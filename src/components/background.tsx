import React from "react";

import * as styles from './background.module.scss';

const Background = ({ width, height }): JSX.Element => (
  <div className={styles.container} />
);

export default Background;
